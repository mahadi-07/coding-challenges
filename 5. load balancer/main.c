#include <stdio.h>

/**
 * In programming, socket programming in C is a method that allows two
 * software programs (nodes) to communicate with each other over a network
 * or on the same computer. It operates on a client-server model: one program acts as the server
 * listening for connections, while the other acts as the client reaching
 * out to initiate one.
 */
int main()
{

    return 0;
}